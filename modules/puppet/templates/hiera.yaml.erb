# This file is controlled by puppet. Do NOT edit! #
---
:backends:
  - yaml
  - gpg
 
:logger: console
 
:hierarchy:
  - passwords
  - "%{operatingsystem}"
  - common
 
:yaml:
  :datadir: <%= scope.lookupvar( "puppet::params::hierapath" ) %>

:gpg:
  :datadir: <%= scope.lookupvar( "puppet::params::hierapath" ) %>
  :key_dir: <%= scope.lookupvar( "puppet::params::gpgpath" ) %>
